/**
 *
 * Header
 *
 */

import React, { memo } from 'react';
// import PropTypes from 'prop-types';
import './style.css'

function Header() {
  return (
    <div>
    </div>
  );
}

Header.propTypes = {};

export default memo(Header);
